
<form class="the-form">
      <h1>Login</h1>
      {{input type="text" class="form-input" placeholder="Enter your email"}}
      {{input type="password" class="form-input" placeholder="Enter your email"}}
      <button class="submit-btn" type="submit">Login</button>
      <label class="form-label">{{#link-to "register"}}Register{{/link-to}}</label>
</form>
